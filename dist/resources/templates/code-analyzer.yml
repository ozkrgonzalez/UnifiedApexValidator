engines:
  pmd:
    file_extensions:
      apex:
        - ".cls"
        - ".trigger"
  cpd:
    file_extensions:
      apex:
        - ".cls"
        - ".trigger"
    minimum_tokens:
      apex: 100
    skip_duplicate_files: true

  eslint:
    disable_engine: true
  retire-js:
    disable_engine: true
  flow:
    disable_engine: true
  regex:
    disable_engine: true
  sfge:
    disable_engine: true

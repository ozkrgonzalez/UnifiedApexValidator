# Step 1 · Check Dependencies

- Open the **Dependencies** view from the Unified Apex Validator activity bar.
- Press **Refresh** to re-run the checks.
- If any dependency shows a warning, click **Update dependency** to follow the suggested fix.

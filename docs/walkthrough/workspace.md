# Step 5 · Point To Your Project Folders

- Set `sfRepositoryDir` to the local path that contains your Apex metadata.
- Choose an `outputDir` where UAV should save HTML/PDF reports.
- Update `sfCliPath` only if the Salesforce CLI is not in your system `PATH`.
